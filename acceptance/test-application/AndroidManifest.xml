<?xml version="1.0"?>
<!--
  To get your application working with Alooma,
  you will need to make some changes to your application manifest file.

  You can use this file as a model for the sorts of changes required. Be aware that
  you will need to replace "com.alooma.example.hello" with your application package
  name in several places before your application will work.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.alooma.example.hello"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="9"
        android:targetSdkVersion="23" />

    <!--
      This permission is required to allow the application to send events and properties to Alooma.
     -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!--
      This permission is optional but recommended so we can be smart about when to send data.
     -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!--
      This permission is optional but recommended so events will contain information about bluetooth state
    -->
    <uses-permission android:name="android.permission.BLUETOOTH" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <!-- to support 2.2 (Froyo) and older devices, your application should allow fallback
             to HTTP if modern HTTPS certificates aren't supported. -->
        <meta-data android:name="com.alooma.android.MPConfig.DisableFallback"
               android:value="false" />

        <!-- The Alooma library will automatically attempt to show available in
             app notifications every time a new Activity is displayed. To disable this behavior,
             add the following line. The default value for this option is set to true. -->
        <meta-data android:name="com.alooma.android.MPConfig.AutoShowAloomaUpdates"
               android:value="false" />

        <!-- This will enable various debugging messages that shows communication between the Alooma
             Android library and the Alooma API servers, which are useful for seeing when track requests
             go out and when notifications are received. -->
        <meta-data android:name="com.alooma.android.MPConfig.EnableDebugLogging"
            android:value="true" />

        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
